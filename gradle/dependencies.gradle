ext {
    versions = [:]
    libs = [:]
}

versions += [
        json    : '20190722',
        jackson : '2.10.1',
        unirest : '1.4.9',
        mockk   : '1.9.3',
        cucumber: '4.2.6',
        kotlin  : '1.3.61',
        jupiter : '5.3.1'
]

libs += [
        json         : "org.json:json:${versions.json}",
        jackson      : "com.fasterxml.jackson.core:jackson-databind:${versions.jackson}",
        unirest      : "com.mashape.unirest:unirest-java:${versions.unirest}",
        mockk        : "io.mockk:mockk:${versions.mockk}",
        cucumberjava : "io.cucumber:cucumber-java:${versions.cucumber}",
        cucumberjunit: "io.cucumber:cucumber-junit:${versions.cucumber}",
        kotlinjunit  : "org.jetbrains.kotlin:kotlin-test-junit:${versions.kotlin}",
        jupiter      : "org.junit.jupiter:junit-jupiter-api:${versions.jupiter}",
        jupiterEngine: "org.junit.jupiter:junit-jupiter-engine:${versions.jupiter}",
        kotlin       : "org.jetbrains.kotlin:kotlin-stdlib-jdk8:${versions.kotlin}"
]
